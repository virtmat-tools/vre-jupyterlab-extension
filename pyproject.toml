[build-system]
requires = ["jupyter_packaging>=0.12,<2", "setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["labextension", "lib", "node_modules"]
